# desktop

package { 'octave':
	ensure => installed,
}

package { 'gnumeric':
	ensure => installed,
}

package { 'r-base':
	ensure => installed,
}

package { 'ipython-notebook':
	ensure => installed,
}

package { 'python-sympy':
	ensure => installed,
}

package { 'python-pandas':
	ensure => installed,
}

package { 'texlive':
	ensure => installed,
}

package { 'texworks':
	ensure => installed,
}

package { 'gnome-dictionary':
	ensure => installed,
}

package { 'inkscape':
	ensure => installed,
}

package { 'gimp':
	ensure => installed,
}


